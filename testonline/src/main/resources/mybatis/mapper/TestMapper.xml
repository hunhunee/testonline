<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sie.mapper.TestMapper">
    <select id="test" parameterType="int" resultType="com.sie.domain.User">
        SELECT * from user where  id=#{id}
    </select>
</mapper>